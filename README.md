# piq
grande projeto
1) README simples (útil e direto)
```md
# Piquepika

Site portfólio simples criado por Patrik.

## Como rodar localmente

1. Clone o repositório:
```bash
git clone https://github.com/patrik/piquepika.git
```
2. Abra o arquivo `index.html` no navegador (dois cliques) ou rode um servidor simples:
```bash
# usando Python 3
python -m http.server 8000
# depois abra http://localhost:8000